export const enum RTByteFormatters {
	AndList = 'andList',
	OrList = 'orList',
	Permissions = 'permissions'
}

export const enum RTByteLanguages {
	EnUs = 'en-US'
}

export const enum FirestoreCollections {
	Client = '/client',
	Guilds = '/guilds'
}
